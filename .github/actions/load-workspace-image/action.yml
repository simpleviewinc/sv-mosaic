name: Load workspace image
description: Download the shared mosaic-workspace Docker image artifact and load it

runs:
  using: composite
  steps:
    - name: Download workspace image
      uses: actions/download-artifact@v4
      with:
        name: workspace-image

    - name: Docker load
      shell: bash
      run: gunzip -c workspace-image.tar.gz | docker load
