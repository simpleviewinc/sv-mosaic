# Avoid shamefully-hoist (flattens everything and causes duplicate @types/react graphs).
# Storybook's Vite builder still needs its packages reachable from the workspace root.
public-hoist-pattern[]=*storybook*
public-hoist-pattern[]=*storybook/*
strict-peer-dependencies=false
