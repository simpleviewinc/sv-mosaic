services:
  workspace:
    image: mosaic-workspace:local
    build: .
    working_dir: /app
    volumes:
      - /app/node_modules
      - /app/packages/mosaic/node_modules
      - /app/packages/storybook/node_modules

  mosaic-units:
    container_name: mosaic-units
    extends: workspace
    environment:
      - CI=true
    command: ["pnpm", "--filter", "@simpleview/sv-mosaic", "test", "--", "--run"]

  mosaic-dev:
    container_name: mosaic-dev
    extends: workspace
    volumes:
      - ./packages/mosaic/src:/app/packages/mosaic/src
      - mosaic-dist:/app/packages/mosaic/dist
    command: ["pnpm", "--filter", "@simpleview/sv-mosaic", "start"]

  mosaic-publish:
    container_name: mosaic-publish
    extends: workspace
    environment:
      - CIRCLE_BRANCH=${CIRCLE_BRANCH}
      - CIRCLE_SHA1=${CIRCLE_SHA1}
      - NPM_TOKEN_GRANULAR=${NPM_TOKEN_GRANULAR}
    command: ["pnpm", "--filter", "@simpleview/sv-mosaic", "publish:npm"]

  storybook-typecheck:
    container_name: storybook-typecheck
    extends: workspace
    command: ["pnpm", "--filter", "@simpleview/sv-mosaic-storybook", "types"]

  storybook-dev:
    container_name: storybook-dev
    extends: workspace
    volumes:
      - ./packages/mosaic/src:/app/packages/mosaic/src
      - ./packages/storybook/stories:/app/packages/storybook/stories
      - mosaic-dist:/app/packages/mosaic/dist
    depends_on:
      - mosaic-dev
    ports:
      - 10001:10001
    command: ["pnpm", "--filter", "@simpleview/sv-mosaic-storybook", "start"]

  storybook-serve:
    container_name: storybook-serve
    extends: workspace
    ports:
      - 10001:10001
    command: ["bash", "-c", "npx --yes http-server ./packages/storybook/storybook-static -p 10001 --silent"]

  storybook-publish:
    container_name: storybook-publish
    extends: workspace
    environment:
      - CIRCLE_BRANCH=${CIRCLE_BRANCH}
    volumes:
      - /home/circleci/.ssh:/root/.ssh
    command: ["pnpm", "--filter", "@simpleview/sv-mosaic-storybook", "publish:init"]

  consumer-tests:
    container_name: consumer-tests
    extends: workspace
    command: ["pnpm", "--filter", "@simpleview/sv-mosaic-consumer-tests", "types"]

  e2e-tests:
    container_name: e2e-tests
    image: mosaic-e2e:local
    build:
      context: .
      dockerfile: packages/e2e/Dockerfile
    environment:
      - CI=true
    depends_on:
      - storybook-serve
    command: ["./scripts/start"]

volumes:
  # Shares the mosaic watch-build output from mosaic-dev with storybook-dev.
  mosaic-dist:
