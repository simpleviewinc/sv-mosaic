import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import * as stories from "./CheckboxList.stories";

<Meta title="Components/CheckboxList/Readme"/>

# CheckboxList

`CheckboxList` is for displaying a list of labeled checkboxes. Clicking the label or the checkbox will select the item.

If you need a dropdown menu with a list of checkboxes use MenuSelect instead, that wraps this component but provides dropdown capability.

## Props

https://github.com/simpleviewinc/sv-mosaic/blob/develop/src/components/CheckboxList/CheckboxList.tsx

## Example

<Preview withSource="none">
	<stories.example/>
</Preview>