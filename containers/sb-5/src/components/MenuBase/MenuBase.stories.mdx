import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

<Meta title="Components/MenuBase/Readme"/>

# MenuBase

`MenuBase` is a micro-component. This can be used when `Menu` is too restrictive, or you are creating or your menu-based mechanic. It just handles the opening of the MaterialUI `Menu`. It can be used in conjunction with `MenuSelect`.

## Props

https://github.com/simpleviewinc/sv-mosaic/blob/develop/src/components/MenuBase/MenuBaseTypes.ts