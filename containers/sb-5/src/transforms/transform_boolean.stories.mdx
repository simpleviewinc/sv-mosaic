import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

<Meta title="Utils/Transforms/Boolean"/>

# transform_boolean

Converts boolean into more friendly formats.

#### Conversions

* `true` => `"Yes"`
* `false` => `"No"`

#### Args

None

#### Example

```js
import { transform_boolean } from "@simpleview/sv-mosaic"
...
transforms : [
    transform_boolean()
]
```
