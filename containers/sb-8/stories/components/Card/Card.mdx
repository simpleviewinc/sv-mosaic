import { Canvas, Meta } from '@storybook/blocks';
import * as stories from './Card.stories';

<Meta of={stories} />

# Card

The `Card` component its a wrapper that will render any content that receives as the following RecentActivity component.

## Props

https://github.com/simpleviewinc/sv-mosaic/blob/develop/containers/mosaic/src/components/Card/CardTypes.ts

## Example

<Canvas of={stories.Playground} />

## Recent Activity

<Canvas of={stories.RecentActivity} />
