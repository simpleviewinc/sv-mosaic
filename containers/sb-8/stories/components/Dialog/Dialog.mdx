import { Canvas, Meta } from '@storybook/blocks';
import * as stories from './Dialog.stories';

<Meta of={stories} />

# Dialog

The `Dialog` component is built over a wrapper for [Material-UI Dialog](https://mui.com/components/dialogs/) but with SimpleView brand colors.

A dialog is a type of modal window that appears in front of app content to provide critical information or ask for a decision.
Is a warning states: It’s possible to use modal windows when the user is about to take an action that has serious consequences and
is difficult or impossible to reverse.

## Props

https://github.com/simpleviewinc/sv-mosaic/blob/develop/src/components/Dialog/DialogTypes.tsx

## Dialog Example

<Canvas of={stories.Playground} />
