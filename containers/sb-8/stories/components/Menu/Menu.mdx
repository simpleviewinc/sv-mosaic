import { Canvas, Meta } from '@storybook/blocks';
import * as stories from './Menu.stories';

<Meta of={stories} />

# Menu

`Menu` is a component that allows you to invoke a dropdown of MenuItems. Each menuItem can have it's own unique `onClick` handler.

## Props

* anchorEl - HTMLElement
* open - boolean
* onClose - function
* items - Array of [MenuItemProps](https://github.com/simpleviewinc/sv-mosaic/blob/develop/containers/mosaic/src/components/MenuItem/MenuItemTypes.ts)

## Basic

<Canvas of={stories.Playground} />

## With Icons and Colors

<Canvas of={stories.WithIcons} />
