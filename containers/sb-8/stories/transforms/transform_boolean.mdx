import { Meta  } from '@storybook/blocks';

<Meta title="Utils/Transforms/Boolean"/>

# transform_boolean

Converts boolean into more friendly formats.

#### Conversions

* `true` => `"Yes"`
* `false` => `"No"`

#### Args

None

#### Example

```js
import { transform_boolean } from "#mosaic"
...
transforms : [
    transform_boolean()
]
```
