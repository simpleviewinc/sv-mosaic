import { Meta  } from '@storybook/blocks';
import { transform_chips } from "#mosaic/transforms/column_transforms";
import { TransformExample } from "#mosaic/transforms/utils/TransformExample";

<Meta title="Utils/Transforms/Chips"/>

# transform_chips

Converts an array of MosaicLabelValue elements into Chips.

#### Conversions

* `[{label: "Chip 1", value: "chip_1"}]` => <TransformExample cb={transform_chips()} data={[{label: "Chip 1", value: "chip_1"}]} />

#### Args

None

#### Example

```js
import { transform_chips } from "#mosaic"
...
transforms : [
    transform_chips()
]
```
