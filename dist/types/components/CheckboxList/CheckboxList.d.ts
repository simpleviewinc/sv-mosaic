import { ReactElement } from 'react';
import { CheckboxListProps } from './CheckboxListTypes';
declare const CheckboxList: (props: CheckboxListProps) => ReactElement;
export default CheckboxList;
