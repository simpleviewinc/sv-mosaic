import { ReactElement, ReactNode } from 'react';
interface InputWrapperProps {
    children: ReactNode;
}
declare const InputWrapper: ({ children }: InputWrapperProps) => ReactElement;
export default InputWrapper;
