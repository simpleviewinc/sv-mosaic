CONTAINER_ID=$( \
    docker container create \
    \ -it
    --name sv-mosaic
    sv-mosaic \
)

docker container start -i sv-mosaic

EXPORT_STRING="export CONTAINER_ID=${CONTAINER_ID}"

echo "Created container ${CONTAINER_ID}"
echo "${EXPORT_STRING}" >> "$BASH_ENV"
