import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import * as stories from "./FilterSingleSelect.stories";

<Meta title="Components|FilterSingleSelect/Readme"/>

# FilterSingleSelect

`FilterSingleSelect` is a filter that can be used to pick a single value from an array of options.

## Props

All filters in `DataView` have a shared props definition. You will need to pass the appropriate `data`, `args` and `onChange` specific to this filter type.

https://github.com/simpleviewinc/sv-mosaic/blob/develop/src/components/FilterSingleSelect/FilterSingleSelectTypes.ts

<Preview withSource="none">
	<stories.example/>
</Preview>