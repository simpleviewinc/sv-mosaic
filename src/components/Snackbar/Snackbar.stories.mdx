import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import * as stories from './Snackbar.stories';

<Meta title='Components/Snackbar/Readme' />

# Snackbar

The `Snackbar` component is built over a wrapper for [Material-UI Snackbar](https://mui.com/components/snackbars/) but with SimpleView brand colors.

Provide brief messages about some specific actions at the bottom of the screen. Used when creating a form or confirming an action like approving an event. 
Appear without warning, and don’t require user interaction. They automatically disappear from the screen after a minimum of four seconds, and a maximum 
of ten seconds.

## Props

https://github.com/simpleviewinc/sv-mosaic/blob/develop/src/components/Snackbar/SnackbarTypes.tsx

## Snackbar Example

<Preview withSource='none'>
  <stories.Example />
</Preview>
