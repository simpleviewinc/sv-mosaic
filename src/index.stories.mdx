import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

<Meta title="Overview/Readme"/>

# sv-mosaic

You have reached the documentation Storybook for sv-mosaic built with https://storybook.js.org/....

* Click between the components in the left navigation, if there is a Readme for the component, start there.
* If you want to see a specific story in isolation, click to the **canvas** tab, you can use the UI controls to either pop the story fullscreen or to a separate tab.
* Some stories have **knobs**, this allows you to dynamically alter the parameters of the story. To use the knobs make sure you're on the **canvas tab** and have **show addons** checked via the "..." in the top left of this screen.
