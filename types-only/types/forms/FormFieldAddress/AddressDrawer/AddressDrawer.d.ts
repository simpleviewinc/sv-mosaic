import { ReactElement } from "react";
import { AddressDrawerProps } from "../AddressTypes";
declare const AddressDrawer: (props: AddressDrawerProps) => ReactElement;
export default AddressDrawer;
